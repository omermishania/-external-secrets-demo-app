apiVersion: 'kubernetes-client.io/v1'
kind: ExternalSecret
metadata:
  name: my-secret
spec:
  vaultRole: "{{ .Values.client_name }}-ro"
  data:
  - key: "{{ .Values.client_name }}/show/my-secret"
    name: matan
    property: matan
