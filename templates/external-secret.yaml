apiVersion: 'kubernetes-client.io/v1'
kind: ExternalSecret
metadata:
  name: SECRET_NAME
spec:
  backendType: vault
  vaultMountPoint: ocp43-dev
  vaultRole: {{ .Values.client_name }}-role
  data:
  - key: {{ .Values.client_name }}/data/{{ .Values.client_name }}
    name: matan
    property: matan
