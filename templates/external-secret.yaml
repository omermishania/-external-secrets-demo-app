apiVersion: 'kubernetes-client.io/v1'
kind: ExternalSecret
metadata:
  name: my-secret
spec:
  vaultRole: "operator-role"
  data:
  - key: "{{ .Values.client_name }}/data/my-secret"
    name: my-secret
    property: my-secret
